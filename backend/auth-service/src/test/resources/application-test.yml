spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6379

app:
  jwt:
    secret: test-jwt-secret-key-must-be-at-least-32-bytes-long-for-hmac256
    expiration: 86400000

twilio:
  account-sid:
  auth-token:
  phone-number:
